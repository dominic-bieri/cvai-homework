services:
  app:
    image: heartexlabs/label-studio:latest
    container_name: label-studio
    ports:
      - "8080:8080"
    volumes:
      - ./labelstudio-data:/label-studio/data
      - ./data/images:/label-studio/data/media/upload/1
    restart: unless-stopped
